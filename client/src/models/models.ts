export interface User {
    name: string;
    email: string;
    password: string;
  }

  export interface Podcast {
      id: number;
      title: String;
      listen: String;
      website: String;
      description: String;
      producer: String;
      children: String;
  
  }
